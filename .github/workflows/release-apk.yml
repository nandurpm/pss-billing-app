name: Build Signed Release APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - release-request.txt

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Validate signing secrets
        env:
          KEYSTORE_BASE64: ${{ secrets.PSS_RELEASE_KEYSTORE_B64 }}
          STORE_PASSWORD: ${{ secrets.PSS_RELEASE_STORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.PSS_RELEASE_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.PSS_RELEASE_KEY_PASSWORD }}
        run: |
          failed=0
          [ -n "$KEYSTORE_BASE64" ] || { echo "::error title=Missing release secret::PSS_RELEASE_KEYSTORE_B64 is not configured"; failed=1; }
          [ -n "$STORE_PASSWORD" ] || { echo "::error title=Missing release secret::PSS_RELEASE_STORE_PASSWORD is not configured"; failed=1; }
          [ -n "$KEY_ALIAS" ] || { echo "::error title=Missing release secret::PSS_RELEASE_KEY_ALIAS is not configured"; failed=1; }
          [ -n "$KEY_PASSWORD" ] || { echo "::error title=Missing release secret::PSS_RELEASE_KEY_PASSWORD is not configured"; failed=1; }
          [ "$failed" -eq 0 ] || exit 1

          mkdir -p "$RUNNER_TEMP/pss-signing"
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/pss-signing/PurpleSignatureRelease.jks"
          test -s "$RUNNER_TEMP/pss-signing/PurpleSignatureRelease.jks" || {
            echo "::error title=Invalid keystore secret::Decoded keystore is empty"
            exit 1
          }

          keytool -list \
            -keystore "$RUNNER_TEMP/pss-signing/PurpleSignatureRelease.jks" \
            -storepass "$STORE_PASSWORD" \
            -alias "$KEY_ALIAS" >/dev/null || {
              echo "::error title=Signing key validation failed::Check the keystore Base64, store password and key alias"
              exit 1
            }

          echo "Release signing key validated successfully."

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build signed release APK
        env:
          PSS_RELEASE_STORE_FILE: ${{ runner.temp }}/pss-signing/PurpleSignatureRelease.jks
          PSS_RELEASE_STORE_PASSWORD: ${{ secrets.PSS_RELEASE_STORE_PASSWORD }}
          PSS_RELEASE_KEY_ALIAS: ${{ secrets.PSS_RELEASE_KEY_ALIAS }}
          PSS_RELEASE_KEY_PASSWORD: ${{ secrets.PSS_RELEASE_KEY_PASSWORD }}
        run: gradle :app:assembleRelease --no-daemon --stacktrace

      - name: Verify signature and prepare customer package
        run: |
          APK="app/build/outputs/apk/release/app-release.apk"
          test -f "$APK"
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs "$APK"
          mkdir -p release-package
          cp "$APK" "release-package/Purple-Signature-Salon-v2.1.2.apk"
          cat > release-package/CUSTOMER-INSTRUCTIONS.txt <<'EOF'
          PURPLE SIGNATURE SALON BILLING APP
          Version: 2.1.2

          ADMIN PIN
          123

          INSTALLATION
          1. Download Purple-Signature-Salon-v2.1.2.apk.
          2. Open the APK on the Android phone.
          3. Allow installation from this source when Android asks.
          4. Tap Install, then Open.
          5. Open Settings inside the app and change the Admin PIN after handover.

          BACKUP
          Use Settings > Backup & Restore > Export All Data regularly.
          Keep the exported JSON backup in Google Drive, email, or another safe location.
          Export a fresh backup before every app update or phone change.

          IMPORTANT UPDATE RULE
          DO NOT UNINSTALL THE APP DURING UPDATES.
          Install the newer APK directly over the existing app. Uninstalling can remove local bills and settings.
          EOF
          sha256sum "release-package/Purple-Signature-Salon-v2.1.2.apk" > release-package/SHA256SUMS.txt

      - name: Stage signed APK for secure retrieval
        run: |
          APK="release-package/Purple-Signature-Salon-v2.1.2.apk"
          OUT="release-staging/signed-v2.1.2"
          rm -rf "$OUT"
          mkdir -p "$OUT/chunks"
          base64 -w0 "$APK" > "$OUT/all-base64.txt"
          split -b 8000 -d -a 3 --additional-suffix=.txt "$OUT/all-base64.txt" "$OUT/chunks/chunk-"
          rm "$OUT/all-base64.txt"
          find "$OUT/chunks" -name 'chunk-*.txt' -printf '%f\n' | sort > "$OUT/chunks/manifest.txt"
          sha256sum "$APK" > "$OUT/SHA256SUMS.txt"
          cp release-package/CUSTOMER-INSTRUCTIONS.txt "$OUT/CUSTOMER-INSTRUCTIONS.txt"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f "$OUT"
          git commit -m "Stage signed Purple Signature v2.1.2 APK" || exit 0
          git push

      - name: Upload customer release package
        uses: actions/upload-artifact@v4
        with:
          name: Purple-Signature-Salon-v2.1.2-release
          path: release-package/
          retention-days: 90
